echo Starting Bare Server
npx bare-server-node
echo NPX Initialized
npx bare-server-node --port 8080 --host localhost
echo Server is running on: PORT:8080 on REPL.LOCALHOST or visit your repl and see the changes